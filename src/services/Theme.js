import { createMuiTheme } from '@mui/material';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#009FE3',
    },
    secondary: {
      main: '#00A19A',
    },
  },
});

export default theme;
